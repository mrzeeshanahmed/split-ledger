# API Configuration
# URL for backend API - defaults to /api if not set
VITE_API_URL=/api

# Stripe Configuration
# Publishable key for Stripe Elements (starts with pk_)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
